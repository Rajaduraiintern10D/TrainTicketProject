@* @model string

<!DOCTYPE html>
<html>
<head>
    <title>Storing Token in Session Storage</title>
</head>
<body>
    <script>
        // Store the token in session storage
        var token = "@Model"; // Retrieve token passed from the controller
        sessionStorage.setItem("JWTToken", token);
        // Redirect to the desired page
        window.location.href = "/Passenger/Passenger"; // Redirect to Passenger controller and Passenger action
    </script>
</body>
</html>
 *@